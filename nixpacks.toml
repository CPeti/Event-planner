[providers]
nodejs = "18"
python = "3.11"

[build.nodejs.build]
cmds = [
  "cd frontend && npm install && npm run build && cd .."
]

[build.python.build]
cmds = [
  "cd backend && pip install -r requirements.txt && cd .."
]
